package com.stream.example;

import java.util.stream.Stream;

/**
 * This class aims to show how you can map a stream from a type to another one.
 */
public class MapStream {

    /**
     * This method should map a stream of double to a stream of integer
     *
     * @param toMap stream to map
     * @return mapped stream
     */
    public static final Stream<Integer> convert(final Stream<Double> toMap) {
        return null;
    }

}